title: User
type: object
x-tags:
  - persons
description: 'Person, who educating in university'
properties:
  id:
    type: string
  account:
    $ref: ./User.yaml
  group:
    $ref: ./Group.yaml
  role:
    $ref: ./Role.yaml
  sex:
    $ref: ./Sex.yaml
  firstName:
    type: string
  lastName:
    type: string
  avatarLink:
    type: string
required:
  - id
  - account
  - group
  - role
  - sex
  - firstName
  - lastName
