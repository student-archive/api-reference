title: User
type: object
description: User of the system
properties:
  id:
    type: string
  account:
    $ref: ./Account.yaml
  group:
    $ref: ./Group.yaml
  role:
    $ref: ./Role.yaml
  firstName:
    type: string
  lastName:
    type: string
required:
  - id
  - account
  - role
  - firstName
  - lastName
