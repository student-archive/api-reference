title: EventPriority
type: object
description: The level of access (system-wide and local per group) and the importance of the event
properties:
  id:
    type: string
  priorityName:
    type: string
  priorityDescription:
    type: string
required:
  - id
  - priorityName
