title: EventPriority
type: object
description: The level of access (system-wide and local per group) and the importance of the event
properties:
  id:
    type: string
  priority:
    type: string
  description:
    type: string
required:
  - id
  - priority
x-tags:
  - events
