title: InviteCode
type: object
x-tags:
  - persons
description: Unique one-time code to add new users
properties:
  id:
    type: string
  account:
    $ref: ./Account.yaml
  inviteCode:
    type: string
  isValid:
    type: boolean
  activatedDate:
    type: integer
required:
  - id
  - inviteCode
  - isValid
